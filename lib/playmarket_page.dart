import 'package:flutter/material.dart';

class PlayMarket extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('MARKET'),),
    );
  }
}